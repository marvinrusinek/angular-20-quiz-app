# Quiet install output (esp. in StackBlitz): hide transitive-dependency
# deprecation warnings, the funding banner, and the audit summary. These are
# warn-level messages from nested deps we can't update directly.
loglevel=error
fund=false
audit=false
